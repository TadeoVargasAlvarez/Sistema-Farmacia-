@model List<FarmaciaSalacor.Web.Models.Transferencia>
@{
    ViewData["Title"] = "Historial de transferencias";
    DateTime? desde = (DateTime?)ViewBag.Desde;
    DateTime? hasta = (DateTime?)ViewBag.Hasta;
}

<div class="erp-page-header">
    <div class="erp-page-title">Transferencias &raquo; Historial</div>
    <div class="erp-page-subtitle">Últimas 200 transferencias</div>
</div>

<div class="erp-panel mb-2">
    <div class="erp-panel-header">Filtros</div>
    <div class="erp-panel-body">
        <form method="get" class="row g-2">
            <div class="col-md-2">
                <label class="form-label form-label-sm">Desde</label>
                <input class="form-control form-control-sm" type="date" name="desde" value="@(desde?.ToString("yyyy-MM-dd") ?? "")" />
            </div>
            <div class="col-md-2">
                <label class="form-label form-label-sm">Hasta</label>
                <input class="form-control form-control-sm" type="date" name="hasta" value="@(hasta?.ToString("yyyy-MM-dd") ?? "")" />
            </div>
            <div class="col-md-8 d-flex align-items-end justify-content-end gap-2">
                <a class="btn btn-secondary btn-sm" asp-action="Historial">Limpiar</a>
                <button class="btn btn-primary btn-sm" type="submit">Aplicar</button>
            </div>
        </form>
    </div>
</div>

<div class="erp-panel">
    <div class="erp-toolbar">
        <a class="btn btn-success btn-sm" asp-action="Nueva"><i class="fa-solid fa-plus"></i> Nueva</a>
        <div class="ms-auto text-muted small align-self-center">Total: <strong>@Model.Count</strong></div>
    </div>
    <div class="erp-panel-body p-0">
        <table class="table table-sm table-striped table-hover mb-0 erp-table">
            <thead>
                <tr>
                    <th style="width:90px;">ID</th>
                    <th style="width:170px;">Fecha</th>
                    <th style="width:140px;">Documento</th>
                    <th>Desde</th>
                    <th>Hacia</th>
                    <th style="width:120px;"></th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count == 0)
                {
                    <tr><td colspan="6" class="text-muted small p-2">No hay transferencias.</td></tr>
                }
                else
                {
                    foreach (var t in Model)
                    {
                        <tr>
                            <td>@t.Id</td>
                            <td>@t.Fecha.ToString("yyyy-MM-dd HH:mm")</td>
                            <td>@(t.Documento ?? "-")</td>
                            <td>@(t.DesdeAlmacen?.Nombre ?? "-")</td>
                            <td>@(t.HaciaAlmacen?.Nombre ?? "-")</td>
                            <td class="text-end">
                                <a class="btn btn-primary btn-sm" asp-action="Detalle" asp-route-id="@t.Id"><i class="fa-solid fa-eye"></i></a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
